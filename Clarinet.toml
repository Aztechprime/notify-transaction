[project]
name = "notify-transaction"
authors = ["Your Name <your.email@example.com>"]
description = "A Clarity smart contract for tracking cross-application transaction events"
telemetry = false
requirements = []
[contracts.transaction-notifier]
path = "contracts/transaction-notifier.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
